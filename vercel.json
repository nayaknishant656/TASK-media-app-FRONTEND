{
    "buildCommand": "cd task-frotend && npm install && CI=false npm run build",
    "outputDirectory": "task-frotend/build",
    "framework": "create-react-app",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}